public class CoordinatePoint { //This class is so we can get the coordinates of each station and then swap each station later.
    int x, y;

    public CoordinatePoint(int x, int y) {
        this.x = x;
        this.y = y;
    }
}
